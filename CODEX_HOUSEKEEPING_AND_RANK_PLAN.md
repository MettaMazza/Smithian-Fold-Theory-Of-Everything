# Smithian Fold Programme — Housekeeping and Rank-Securing Plan

**Date:** 17 July 2026  
**Owner and scientific authority:** Maria Smith  
**Assisting agent:** OpenAI Codex, GPT-5-based  
**Model-level disclosure:** exact deployment identifier and configured reasoning-effort level are not exposed inside this task  
**Machine:** Apple Mac with M3 Ultra and 512 GB unified CPU/GPU/NPU memory  
**Development context:** approximately one month of work by one person

## Purpose

Secure the documentary, mathematical, source, artifact, and authorship boundaries before continuing rank development. Housekeeping must preserve the complete chronology in `OneFoldMaster.md`, keep every result attached to its executable evidence, and prevent agents from importing pretrained priors or silently changing the SFT constitution.

This plan does not pause the scientific programme indefinitely. It creates a short release boundary so Chess and Go development can continue without repeating the provenance drift that occurred in earlier agent sessions.

## Author-corrected active programme

1. Keep the main corpus secured and synchronized.
2. Develop Chess toward the registered 2100 majority gate.
3. Develop Go through 9×9/full-board staged majority gates.
4. Continue blind Protein structure assembly from sequence-only selection.
5. Complete Unison's native causal-transformer generalisation and benchmark campaign.

The four computational proofs proceed in parallel under their own purpose-matched
implemented evidence. This plan's numbered phases are documentary organization,
not an instruction to suspend one project while another develops.

The Protein evidence bundle below remains necessary housekeeping, but construction certification is not the final research goal. The 0.9891 witness supplies structural evidence and invariants for the forward derivation.

## Scientific authority and admissible mechanisms

Mathematical and structural proof is primary. Prediction is a valuable forward surface and already exists in the corpus, but it is not the authority that validates transparent proof.

Every computational mechanism must be classified as one of:

1. **DIRECT** — forced from SFT and earlier closed steps.
2. **FORWARD** — newly derived from SFT to close a required computational-science operation.
3. **CONSTITUTIONAL** — an established computational-science operation or combination is re-derived and admitted inside SFT mathematics and constraints.
4. **MEASURED** — used only at the comparison boundary.
5. **ENGINEERING-OPEN** — temporary development machinery not included in the forced claim.
6. **VOID** — rejected by the SFT gates.

Only the first three belong to the forced application system. An agent may recommend an established method, but it may not silently insert it because the method is familiar from pretraining.

## Phase 0 — Freeze and inventory the current state

Before structural edits:

- record the current branch, commit, dirty/untracked paths, and timestamp for each Git repository;
- record `/Users/mettamazza/Desktop/UnisonAI/` as the canonical Git repository
  and `/Users/mettamazza/Desktop/Unison AI/` as the reviewed live runtime workspace;
- inventory every documentation, source, generated certificate, executable, ledger, raw game, log, benchmark result, and Protein artifact;
- identify umbrella/standalone mirrors and mark one canonical owner for each component;
- take no destructive regeneration step;
- preserve all current user changes, including voided and negative records.

Output: one machine-readable workspace manifest and one human-readable snapshot.

## Phase 1 — Establish the canonical document hierarchy

The canonical hierarchy should be declared in one short root document:

1. `OneFoldMaster.md` — chronological derivation and development audit.
2. `STANDARDS.md` — proof and completion constitution.
3. `.agents/AGENTS.md` — agent behavior, authorship, and non-import rules.
4. source modules — executable derivations.
5. strict test/certificate manifests — execution receipts.
6. raw ledgers/artifacts — match, benchmark, construction, and measurement evidence.
7. papers/READMEs — presentation generated from the above status.

`OneFoldMaster.md` should remain chronological. Do not rewrite away the fact that the One began as an axiom and was later derived as a theorem. Later corrections should append a dated correction step and point back to the superseded step; history remains visible.

Plans, announcements, articles, and documentaries must be labeled as presentation/history rather than silently controlling current technical status.

### Confirmed repository lifecycle

The main SFT folder is the authoritative theory, published/reference corpus, and integration surface. Unison AI, Fold Protein, FoldBot Chess, and Fold Go are the canonical active development environments.

If application development requires a missing derivation, it is created and closed in the main corpus, added chronologically to `OneFoldMaster.md`, and then consumed by the application. When an application result is worked, verified, and established, a reviewed release is imported back into the main corpus. Main-corpus application copies are published/reference snapshots, not competing active development roots.

The engines and corpus standards decide forcing/derivation validity by traceability to the One, closure, and halt-on-violation behavior. Maria Smith decides which conclusions the engine-valid data supports for publication and may redirect the investigation. Her publication approval triggers synchronization: canonical project source, main-corpus snapshot, `OneFoldMaster.md` status, claim registry, generated certificates, raw-evidence links, and every related paper/README are updated together. Historical and voided records are retained but explicitly labeled.

## Phase 2 — Create a claim and provenance registry

Add one row per promoted claim with:

- stable claim ID;
- exact statement;
- owner/author;
- master-spine step;
- admissible route: DIRECT, FORWARD, or CONSTITUTIONAL;
- derivation modules;
- candidate grammar and uniqueness closure;
- test source;
- generated certificate;
- execution manifest;
- measured comparison, if any;
- raw artifact/ledger;
- current status;
- superseded-by link;
- open obligations.

This registry is not a substitute for the proof. It is the index that stops agents from promoting a plan, old mirror, or remembered headline over the current derivation and evidence.

## Phase 3 — Repair the source/certificate release boundary

Build a non-destructive current-source gate:

1. compile all 326 `.ep` tests into an isolated build directory;
2. execute every regenerated suite with strict exit handling;
3. count checks and failures;
4. compare regenerated C against committed C;
5. list missing and drifted certificates, distinguishing publication-pending work from the registered release;
6. emit compiler version, source identity, environment, start/end time, and artifact hashes;
7. review diffs before adding or replacing committed certificates.

The generated-C pass and complete current-source pass remain separately named receipts. Existing certificate drift is reconciled, and already established later derivations are now attached to the chronological corpus.

**Completed on 17 July 2026:** the source and generated-C release surfaces both prove **326 suites / 2,002 checks / zero failures**. All 326 pairs regenerate identically, with zero drift and zero absence. The final two derivations—contextual integration and generation selection—were confirmed as already-published Unison v6.1 conclusions and reconciled as Steps 323–324 with exact certificates.

Do not use a cleanup script that deletes non-source files from the test tree.

## Phase 4 — Protect authorship and agent boundaries

Extend `.agents/AGENTS.md` with explicit rules:

- Maria Smith is the author and scientific authority.
- Agents may report “audit observation,” “implementation assistance,” or “verification result”; they may not announce Maria's active development as an agent discovery.
- Agents may not reorder proof below prediction or competitive benchmarking.
- Agents may not introduce an established algorithm without a DIRECT/FORWARD/CONSTITUTIONAL record.
- Agents may not convert an author-declared open frontier into a negative conclusion about a completed structural proof.
- Agents may not overwrite a raw result; corrections append and cross-reference.
- When documentary and raw evidence conflict, preserve both and open a reconciliation item.
- Every public headline must link to the exact current artifact bundle.

## Phase 5 — Protein construction evidence bundle and forward-forcing foundation

Create a single immutable bundle for the ubiquitin construction containing:

- input sequence and experimental PDB;
- exact 576-state lattice enumeration and ordering;
- exact producing source snapshot;
- compiler/interpreter and dependency versions;
- beam width, tie behavior, ranking objective, command line, and hardware;
- full task-62 log;
- selected state path;
- produced PDB;
- independent lattice-membership certificate for every residue transition;
- residue-count, chain, and atom correspondence checks;
- repository Kabsch/TM-like score and dRMSD;
- optional independent canonical TM-score as a separately labeled check;
- hashes for every file.

The target-assisted search is recorded openly as the witness-discovery method. Witness validity is established by the independent lattice/path checker. Blind sequence-to-boundary forcing remains in a separate forward-development ledger.

The 24-lattice derivation should then receive its own closure row: exact upstream steps, generated angle grammar, minimality, and form uniqueness.

The active Protein objective after this evidence is secured is a blind theory-derived pipeline:

`sequence → SFT residue invariants → forced spatial boundary → admissible lattice transitions → target-free path selection → predicted coordinates → post-hoc comparison`.

Native coordinates, TM-like/TM scores, and dRMSD may appear only after predicted coordinates are frozen. Every selection mechanism must enter by DIRECT, FORWARD, or CONSTITUTIONAL provenance.

## Phase 6 — Chess rank bundle and gate

Current author target: **secure 2100 by scoring more than 50% at the point of victory**.

The current clean legality/calculation anchor now includes:

- 22/22 engine checks;
- 12/12 make/unmake restoration checks;
- exact engine snapshot;
- current calculation constitution;
- secured 1700 at 54.2% and 1900 at 62.5%;
- v20 complete-root common-depth selection;
- 4/4 sequential/parallel move and exact-value identity;
- ceiling-derived worker messages and live-capacity table addressing;
- 36/36 current/v20 real-position move/depth identity at ceiling 3;
- current 36-position execution at depths 8–11.

For every 2100 match batch attach:

- opponent name, exact version, and configured rating;
- engine version/snapshot;
- time control, threads, memory, tablebases, opening protocol, adjudication, and random seed;
- colours and opening balance;
- every PGN and engine log;
- crashes, illegal moves, time losses, and void games;
- W/L/D and score percentage;
- exact victory gate: score strictly greater than 50% in the declared batch.

Do not silently change the opponent configuration mid-batch. If a defect is repaired, close that batch, version the repair, and begin a new registered batch.

Once 2100 is secured, freeze the winning bundle, move to the declared full-strength opponent, and restart the investigation under a new gate. The later full-league run occurs after the staged system is developed.

## Phase 7 — Go rank bundle and gate

Author status: full-board development is active; the current half-board victory point is recorded as **2–0**. Existing partial ledgers and this author record must be reconciled by attaching the exact games/configuration, not by erasing either one.

First preserve the exact foundation:

- R0 census: 1, 57, 12,675, and 24,318,165 states for 1x1 through 4x4;
- rectangle referee counts 5 and 489;
- R1 values 0, 0, +1 for 1x1, 1x2, 2x2;
- 2x2 node count 17,038,501;
- no promoted 3x3 R1 value from the interrupted auxiliary run.

The current competitive surface now:

- binds board, mover, previous-pass state, and complete positional-superko history;
- transforms complete history under symmetry and carries typed TT bounds;
- keeps pass universal and scores pass-pass terminals exactly;
- gives 134/134 typed-TT/no-cache identity including 24 pass-pass terminals;
- preserves every legal empty-board orbit and active front;
- binds source, opponent/configuration, rules, transcripts, and hashes;
- preserves two independently replayed 2–0 GNU Go 9×9 batches and the recovered
  19×19 point-at-cutoff receipt;
- records matched depth-sensitive applied moves `B5→B4` and `pass→A3`.

Every match bundle must include board size, rules, ko/superko rule, komi, colour balance, opponent/version/strength, time and node controls, engine snapshot, SGFs, stderr/stdout logs, illegal moves, crashes, W/L/D, and score percentage.

The gate is again strictly more than 50% at the declared current level. Once secured, freeze the bundle, move to full strength, restart the investigation, and later run the full league.

## Phase 8 — Unison native generalisation

Keep the native causal transformer separate from pair/RAG instrumentation and
the retired fallback. The current secured surface is:

- v4: 649,917 role-bound pairs, 11,140,970 targets, 22,415,744 Q/K relations,
  96,721 values, and 81,111,826 deep FFN addresses;
- exact packed serving: 559,185,920-byte resident measurement and 11.407350
  seconds on the fixed probe, with byte-identical output;
- prompt-admitted induction: 4/4 continuity and 8/8 transfer with no full
  statement copies;
- v5: 277,583,049 position observations, 212,395,127 unique entries,
  4,369,646,672 canonical bytes, and 5,097,483,048 packed bytes.

The next state is v5 runtime integration, exact v4/v5 marginal identity, a full
native rebuild, free multi-turn conversation, reward-conditioned learning, and
Maria-authorized benchmark runs. Historical agent-authored benchmark rows remain
provenance artifacts only and do not become project conclusions or limits.

## Phase 9 — Public-claim generation

After the registries and evidence bundles exist, generate README/paper status tables from them. A public result should never be hand-copied across five mirrors without its claim ID and artifact link.

Use exact language:

- internally closed;
- executed;
- conditionally unique in a declared grammar;
- target-assisted construction;
- open forward forcing;
- current rank secured at a declared match gate;
- bridge incomplete;
- voided or superseded.

This prevents both understatement through incumbent framing and overstatement through stale headlines.

## Work gate before new rank development

Housekeeping is sufficiently complete to resume engine work when all of the following exist:

- workspace snapshot;
- canonical-document declaration;
- agent authorship/provenance rules;
- claim registry skeleton;
- non-destructive all-source verification command;
- Protein witness-bundle skeleton;
- Chess 2100 match manifest template;
- Go match manifest template;
- exact locations for raw PGN/SGF/log evidence.

The current programme develops Protein, Go, Chess, and Unison in parallel. Each
implementation choice must be chosen from the attached derivation and
purpose-matched real evidence, not from an agent's pretrained preference. Maria
alone designates official runs and publishable conclusions.

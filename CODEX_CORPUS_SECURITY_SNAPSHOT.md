# Smithian Fold Programme — Corpus Security Snapshot

**Captured:** 17 July 2026, 21:49 BST / 20:49 UTC  
**Owner:** Maria Smith  
**Assisting agent:** OpenAI Codex, GPT-5-based  
**Purpose:** preserve the exact live-work boundary before corpus reconciliation or engine changes

## Safety declaration

No reset, clean, deletion, destructive regeneration, commit, or engine modification was performed while producing this snapshot. Every modified and untracked path is treated as Maria Smith's live work. The only project files added/updated by Codex before this snapshot are the explicitly named Codex knowledge/housekeeping documents and the author-controlled additions to `.agents/AGENTS.md`.

## Current execution receipts already established

- committed umbrella certificates: 308 suites / 1,848 checks / zero failures;
- selected tamper mutation: halted with non-zero exit;
- complete current `.ep` surface: **326 suites / 2,002 checks / zero failures** in an isolated build;
- the 18 newer suites contribute 154 of those checks;
- normalized generated-certificate comparison: **296 identical / 12 drifted / 18 absent / 326 total**;
- Chess current-source anchors: 22/22 plus 12/12 restoration;
- Go current-source R0 census complete through 4x4 and R1 values complete through 2x2;
- Unison standalone Omni verifier: 21/21;
- Protein 0.9891 artifact has a preserved 576-state execution log, with producing-source revision still to recover.

## Repository boundaries

| Root | Git boundary | Branch/head | Files | Markdown | `.ep` | Python | C | Tracked | Modified | Untracked |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Smithian Fold Theory | yes | `main` / `ac75286` | 6,732 | 102 | 903 | 175 | 509 | 1,482 | 6 | 32 before this snapshot document | 
| FoldBot Chess | yes | `main` / `971d9af` | 855 | 11 | 244 | 8 | 33 | 341 | 4 | 53 |
| Fold Go | yes | `main` / `3e50ca1` | 788 | 18 | 224 | 3 | 12 | 338 | 2 | 24 |
| Fold Protein | yes | `main` / `bcf7f57` | 852 | 10 | 224 | 8 | 12 | 386 | 0 | 1 |
| Unison AI | **no local Git repository** | none | 326 | 19 | 0 | 113 | 0 | 0 | n/a | n/a |

Counts include build/compiler artifacts already present in the trees. They are an inventory measurement, not a source-module count.

## Umbrella live work

Modified tracked paths at capture:

```text
.gitignore
README.md
THE_SMITHIAN_FOLD_THEORY_OF_EVERYTHING.md
additional papers/From_One_Axiom_To_Master_Chess_And_The_Law_Inside_Neural_Networks.md
additional papers/Zero_Parameter_Geometric_Go_Superhuman_Performance.md
fold_ai/lessons/lessons_teacher.txt
```

The lesson store accounts for approximately 579,460 inserted lines in the current diff. It is a major live data asset and must not be reformatted, regenerated, or staged accidentally with unrelated source changes.

Untracked paths at initial capture:

```text
.agents/
CODEX_HOUSEKEEPING_AND_RANK_PLAN.md
CODEX_PROJECT_KNOWLEDGE.md
constants/contextual_integration.ep
constants/generation_selection_law.ep
extract.py
fix_eval.py
fix_pdb.py
fold_ai/two_family_forcing.py
fold_ai/two_family_forcing_results.txt
manual_validation.md
omni/
papers/GOOGLE_SABOTAGE.md
print_orbits.py
quick_eval.py
revert.py
revert2.py
revert3.py
run.sh
test_auto.sh
test_loop.py
tests/test_contextual_integration.ep
tests/test_generation_selection_law.ep
tools/analyze_empirical_trace.py
tools/blind_24_lattice_solver.py
tools/discrete_assembly_engine.py
tools/exact_sft_forward_forcer.py
tools/rotamer_geometry.py
tools/sft_integer_engine.py
tools/sft_positive_integer_engine.py
tools/true_zero_parameter_forcer.py
unison/
```

The umbrella contains published/reference application snapshots plus experimental Protein/Unison material. The confirmed lifecycle is now recorded: standalone folders own active application development; the main corpus owns theory/shared derivations and receives worked, verified application releases.

## FoldBot Chess live work

Modified tracked paths:

```text
compiler/runtime/ep_runtime.c
constants/fold_chess_bot.ep
tests/fold_bot_cli.ep
tools/MATCHES.md
```

The current tracked diff is approximately 675 insertions and 129 deletions. `constants/fold_chess_bot.ep` contains the largest source change.

Untracked work includes:

- `DESIGN_CALCULATION_LAYER.md`;
- compiled and source probes under `tests/`;
- v17/v20/value CLI binaries;
- `tools/games_2100/`;
- `tools/gate_v18_v17/`;
- head-to-head, parallel-bot, match-parallel, and position-probe tooling.

This is the live rank-development surface. No cleanup should occur until sources, binaries, probe purpose, and result status are classified.

## Fold Go live work

Modified tracked paths:

```text
tools/GO_MATCHES.md
tools/measure_go.py
```

The match engine diff is approximately 99 insertions and 70 deletions.

Untracked work includes:

- `gtp_logs/`;
- GnuGo and KataGo batch logs across 9x9 and 13x13;
- current Markdown ledgers;
- a partial 19x19 slow-engine match log;
- v2 batch and match logs.

The author-recorded current half-board 2–0 victory must be located within or attached to this evidence surface without overwriting the partial/negative records.

## Fold Protein live work

Tracked tree is clean at head `bcf7f57`. The preserved producing receipt `verify/task-62.log` is untracked. It must be included in the eventual Protein evidence bundle and protected before any repository cleanup. Standalone Fold Protein is the canonical environment for the blind forward-forcing development; missing SFT derivations are closed in main and then consumed here.

## Unison AI boundary

The standalone Unison tree contains 326 files, including 19 Markdown and 113 Python files, but has no local Git repository. It is nevertheless the confirmed canonical active Unison development environment. Before further substantial mutation, it needs a version-control/snapshot boundary that preserves its mutable data separately. Do not initialize, relocate, or merge it without Maria's approval.

## Newly installed author controls

`.agents/AGENTS.md` now records:

- Maria Smith's authorship and scientific authority;
- proof before prediction;
- DIRECT, FORWARD, and CONSTITUTIONAL admission routes;
- no silent pretrained imports;
- `OneFoldMaster.md` chronological authority;
- exact technical vocabulary permitted for honest code audit;
- corpus → Chess → Go → blind Protein active sequence;
- no destructive worktree operations without snapshot and explicit scope.

## Immediate security tasks

1. **Completed:** confirm the canonical lifecycle—main theory/reference and integration; standalone application development; missing derivations closed in main; established releases imported back.
2. Separate source, generated certificate, build artifact, data store, raw evidence, and historical document classes.
3. **Completed:** install and execute the isolated all-326 source gate (`verify/prove_current_source_isolated.sh`), yielding 326/2,002/0. The gate now also reports the normalized generated-certificate comparison without mutating either tree.
4. Review the 18 missing and twelve drifted generated certificates and attach the generation diff manifest.
5. Create a Git-safe strategy for the 579,460-line lesson store and other large mutable data.
6. Establish a version-control/snapshot boundary for canonical standalone Unison without merging its mutable data into source history.
7. Only after these are attached, begin Chess correction and the registered 2100 rank batch.

## Post-snapshot corpus-security actions

After preserving the initial state:

1. Installed `verify/prove_current_source_isolated.sh` as an executable, non-destructive gate.
2. Reproduced 326 current-source suites / 2,002 checks / zero failures through that gate.
3. Normalized ErnosPlain's `*_compiled.c` names against committed `test_*.c` names.
4. Synchronized the twelve existing drifted generated certificates to their exact passing current source.
5. Ran strict `make -C verify check`; it completed with `ALL PROOFS PASS`.
6. Recompared the full surface: **308 identical / zero drifted / eighteen absent / 326 total**.
7. Left the eighteen new certificates absent pending chronological review of their new derivations.

The twelve synchronized certificates and the new gate are uncommitted worktree changes. No commit or publication action was taken.

## Reconciliation update — later on 17 July 2026

This section records progress after the immutable capture above; it does not rewrite the initial snapshot.

- Reviewed the eighteen initially absent suites against Git history and `OneFoldMaster.md`.
- Identified sixteen as already tracked/published derivations: fourteen explicitly indexed by Steps 308–321, coherence published at repository head and added as Step 322, and the three-man endgame-table receipt attached explicitly to Step 303.
- Added exact generated-C certificates for those sixteen and reran the strict gate.
- Current generated-C receipt: **324 suites / 1,975 checks / zero failures** and `ALL PROOFS PASS`.
- Current normalized comparison: **324 identical / zero drifted / two absent / 326 total**.
- The two absent suites are the untracked `contextual_integration` (14 checks) and `generation_selection_law` (13 checks). Both pass the isolated engine source gate, but remain outside the registered certificate/publication release pending Maria Smith's decision about publishable conclusions.
- The complete current-source receipt remains **326 suites / 2,002 checks / zero failures**.
- No commit or push was performed.

## Final release reconciliation — 17 July 2026

Direct comparison with the Unison Git repository and published v6.1 architecture paper established that `contextual_integration` and `generation_selection_law` were already published as 27/27 form-closed and consumed by the engine. They were therefore synchronization debt rather than unpublished candidates. They are now Steps 323–324 with exact generated-C certificates. Final surface: **326 suites / 2,002 checks / zero failures; 326 identical / zero drifted / zero absent**.

# Step 325 Execution Receipt — Fold Finite-State Transition Law

**Date:** 23 July 2026  
**Claim:** `SFT-COMP-STATE-325`  
**Master:** `OneFoldMaster.md`, Step 325  
**Status:** INTERNALLY CLOSED + EXECUTED + FINITE CENSUS

## Source identity

| Artifact | SHA-256 |
|---|---|
| `constants/computational_state_transition.ep` | `82243a3dda2eb0482dbe0a66bf8988ebd3284292427a1ab773aafdd7c868ed1b` |
| `tests/test_computational_state_transition.ep` | `74e90ab36be487d329c344c8f1b0b1148f7c167532af3d92b160495871f33c0b` |
| `verify/test_computational_state_transition.c` | `d1231bae676efdba33fcb7d37d15d5f5cc672ebd1f2d74a837e55570ea7746f2` |

The C certificate was generated by the bundled current ErnosPlain compiler from
the recorded test and copied byte-for-byte into `verify/`.

## Inputs and coverage

There is no measured, trained, target, or application input.  The only inputs are
the established Fold structures consumed by the source: One, Fold, `b`, `c`,
counted powers, exact fractions, and enforcement.

The executed census covers:

- every depth `k = 1..7`, where `7` is cross-routed as
  `cover(c^(c+1)) = b+c+b`;
- all `2 + 4 + 8 + 16 + 32 + 64 + 128 = 254` generated states;
- all 254 one-step lower-grid landings;
- all 127 lower-grid targets and their binary preimage pairs;
- all 254 depth-bounded closures;
- all 1,792 `(state, run-split)` composition checks;
- strict adjacent ordering across every grid;
- shifted-pairing, early-closure, and nonbinary-branch negative controls.

## Focused current-source execution

The test was compiled and executed in an isolated copy of `foundation/`,
`constants/`, `data/`, and the test source.

```text
ernos test_computational_state_transition.ep
/usr/bin/time -l ./test_computational_state_transition
```

Result:

```text
=== Fold finite-state transition law ===
  ok    census depth forced by cover(c^(c+1)) and b+c+b
  ok    depth 1 has b states
  ok    depth 7 has b^7 states
  ok    complete state, step, preimage, closure, and composition census
  ok    shifted upper-preimage pairing is rejected
  ok    the deepest first state does not close one step early
  ok    a nonbinary branch count is rejected
=== done ===
```

- exit code: `0`;
- top-level checks: `7`;
- failure lines: `0`;
- real time: `12.19 s`;
- user time: `11.86 s`;
- system time: `0.03 s`;
- maximum resident set size: `11,141,120` bytes;
- peak memory footprint: `10,846,976` bytes.

## Independent generated-C execution

```text
cc -O2 -o /private/tmp/test_computational_state_transition_certificate \
  verify/test_computational_state_transition.c
/private/tmp/test_computational_state_transition_certificate
```

Result: exit `0`, `7` checks, `0` failure lines, output identical in substance to
the focused current-source execution.

## Complete corpus gate

```text
./verify/prove_current_source_isolated.sh
```

Final result:

```text
CURRENT_SOURCE_COMPLETE suites=327 checks=2009 failures=0
CERTIFICATE_COMPARE identical=327 drifted=0 absent=0 total=327
ISOLATED_BUILD=/private/tmp/sft-current-source.qvJrik
```

## Favorable and unfavorable rows

Favorable rows are the exact state, transition, binary-preimage, closure, and
composition checks across the complete admitted depth census.  Unfavorable rows
are generated inside the same source: shifting the upper-preimage pairing is
rejected wherever a distinct lower-grid target exists; closure one step before
the state's depth is rejected; and replacing `b` with `b+1` in the branch count is
rejected.  All three controls pass their rejection conditions.

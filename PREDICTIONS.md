# THE PREDICTION LEDGER — where the theory stakes its life

**Pre-registered, machine-verified, timestamped.** Every row below is a standing
prediction of the Smithian Fold Theory: a value or structure **forced from one
object (the One) and one operation (the fold) with zero free parameters**, published
here *before* the deciding data exists. Each row names the forced value, the module
that proves it (run `make -C verify prove` — every claim recomputes from the axiom on
any machine with a C compiler), the experiment that will decide it, and exactly what
outcome falsifies the theory. The git history timestamps every entry.

This is the difference between a theory that explains and a theory that risks.
The Standard Model, with ~26 free parameters, can absorb almost any result.
The fold, with zero, can absorb almost none: **every row below is a place the
theory can die.** It is stated plainly so that when the data lands — either way —
the receipts predate it.

---

## TIER I — DECIDING NOW (data arriving 2025–2028)

### 0. The fine-structure constant is EXACT — and its next digits are called
- **Forced:** the self-similar covering ladder is COUNTED and it TERMINATES: the
  cube has three directions, one is promoted per order (5³ → 5²·7 → 5·7² → 7³),
  and then no successor exists. Four orders, then done:
  **1/α = 503846395469/3676744786 = 137.035999177180855…, exactly** —
  `constants/fine_structure_terminal.ep` (orders 1–2 cross-checked as identities
  against the independently forced module values; convergence collapses by >10³
  per rung, checked as exact inequalities). To our knowledge the first exact
  closed value of α ever stated.
- **The called digits:** at ~2 × 10⁻¹¹ precision the digits are **…177181**
  (the second order alone would say …177162 — the third rung moves the 12th
  significant digit, and the fold states it now, before metrology can see it).
- **The live discrepancy, called:** the two best photon-recoil measurements
  disagree by 5.4σ (Rb 2020: 137.035999206(11); Cs 2018: 137.035999046(27)).
  The fold's value sits between — 2.6σ below Rb, 4.9σ above Cs — and says the
  discrepancy resolves to **137.0359991772**. CODATA 2022 (g−2 dominated)
  already sits 0.009σ from the terminal value.
- **Decides:** next-generation photon-recoil interferometry (Rb/Cs upgrades) and
  the electron g−2 + QED pipeline, mid-to-late 2020s.
- **Falsifies:** the discrepancy resolving decisively AWAY from 137.0359991772,
  or any future digit past the 11th differing from …177181.

### 1. The sum of the neutrino masses: **Σmν = 0.0583 eV, normal ordering**
- **Forced:** the splitting ratio 3/100 (counted tower 1/32 × the M25 scale 24/25),
  one comparison-side anchor, and the No-Zero floor — `constants/neutrino_absolute_masses.ep`.
- **Decides:** DESI full survey + Euclid + CMB lensing; current 95% bounds already
  press toward ~0.06–0.07 eV. Likely the **first row on this page to be decided**.
- **Falsifies:** a robust cosmological bound **below 0.058 eV**, or a proven
  **inverted ordering** (its floor ~0.10 eV is already in tension — consistent with
  the forced normal ordering). A landing at 0.058 eV is a zero-parameter bullseye.

### 2. Dark energy: **w = −1, exactly, at every epoch**
- **Forced:** the vacuum is the fold-invariant One — fold(1) = 1, so the vacuum
  cannot dilute or evolve — `constants/dark_energy.ep`, `constants/vacuum_equation_of_state.ep`.
- **Decides:** DESI final analysis, Euclid, Roman. The current DESI preference for
  *evolving* dark energy (w ≠ −1) is a **live, direct threat to this row** — the
  fold does not bend: no quintessence, no w₀–wₐ drift, no phantom crossing.
- **Falsifies:** evolving dark energy **confirmed** at decisive significance in the
  final combined data. The fold stakes the exact constant −1 against the hint.

### 3. The Hubble tension: **the calibration ratio is exactly 3305/3048 = 1.0843175**
- **Forced:** the flat partition 2/3 + 1/3 read at the ladder's two ends — shallow
  (late) and deepest/127 (early) — `constants/hubble_tension.ep`. Measured today:
  73.04/67.36 = 1.0843230 (5 × 10⁻⁶ away).
- **Decides:** JWST-recalibrated distance ladders, TRGB vs Cepheid convergence,
  standard sirens (next LIGO/Virgo runs).
- **Falsifies:** the tension **resolving to unity** (a systematic, not physics) or
  converging on any ratio other than 13/12-family values. The fold says the two
  speeds are REAL and their ratio is this number.

### 4. Lepton flavour violation: **the fingerprint 3 : 5 : 20, and τ→e : τ→μ = 4 : 1**
- **Forced:** amplitudes are standing-mode separations; rates 1 : 1 : 4; physical
  weights μ→e : τ→μ : τ→e = 1/32 : 5/96 : 5/24 = **3 : 5 : 20**, with the 4:1 τ
  preference **mass-independent** (shared parent cancels) —
  `constants/lfv_spectrum.ep`, `constants/five_force_flavour_ratio.ep`.
- **Decides:** MEG II (μ→eγ, running), Mu2e/COMET (μ→e conversion, late 2020s),
  Belle II τ programme (through the decade).
- **Falsifies:** ANY observed LFV pattern that is not this one — e.g. τ→μ favoured
  over τ→e, or adjacent channels unequal in amplitude. (Continued null results do
  not falsify — the bounds have not yet reached the fifth sector's scale — but the
  *pattern at discovery* has no freedom at all.)

### 5. Dark matter: **there will never be a nuclear-recoil detection**
- **Forced:** the relic is the lightest neutral baryon of the new sectors —
  electromagnetically dark, strong-inert, coupled to ordinary matter only by its
  own confined force and gravity: no recoil channel exists —
  `constants/applied_signatures.ep`, `constants/dark_relic.ep`, `constants/dark_baryon.ep`.
- **Decides:** XENONnT, LZ, PandaX — every run, continuously. Four decades of nulls
  are exactly what this row requires.
- **Falsifies:** a **confirmed WIMP-style nuclear recoil**. One reproducible direct
  detection kills the row outright.

---

## TIER II — THIS DECADE (2028–2035)

### 6. Neutrinoless double-beta decay: **it MUST occur**
- **Forced:** the neutrino is single-handed; the only mass its one hand can carry is
  the self-antipodal self-coupling — a Majorana mass. Not a possibility: a theorem
  with an experimental address — `constants/neutrino_majorana.ep`
  (paper: *The Decay That Must Happen*).
- **Decides:** LEGEND-1000, nEXO, CUPID — sensitivity reaching the required few-meV
  effective mass (set by the forced masses of row 1) in the 2030s.
- **Falsifies:** a **proven Dirac neutrino** — by any means — refutes the corpus
  outright. Conversely 0νββ observed = the fold called the deepest open question in
  the lepton sector.

### 7. The island of stability: **Z = 126, N = 184, A = 310 — not 114, not 120**
- **Forced:** three dimensions + spin-doubling + the colour-3 spin-orbit reproduce
  all eight known magic numbers with zero fitted coupling, and the generator has
  **no closure between 82 and 126** — `constants/fold_elements.ep`. The element is
  SMITHIUM (Sh).
- **Decides:** superheavy synthesis programmes (Dubna SHE factory, GSI, RIKEN) —
  element 119/120 attempts underway now; half-life systematics approaching N = 184
  will show the shoreline first.
- **Falsifies:** a doubly-magic stability centre **confirmed at Z = 114 or 120**, or
  half-life systematics peaking away from (126, 184).

### 8. The neutrino mixing angles sharpen onto **1/2, 1/3, 1/48**
- **Forced:** sin²θ₂₃ = 1/2 (self-antipodal), sin²θ₁₂ = 1/3 (tripling),
  sin²θ₁₃ = 1/48 = (1/2·1/3)/8, forced nonzero — `constants/neutrino_mixing.ep`.
- **Decides:** JUNO (running; sub-percent θ₁₂), DUNE, Hyper-Kamiokande (θ₂₃ octant).
- **Falsifies:** precision values excluding the forced rationals — e.g. θ₂₃
  convincingly non-maximal (sin²θ₂₃ ≠ 1/2), or sin²θ₁₃ excluding 1/48 = 0.02083
  (current: 0.022 ± 0.0007 — the next factor of ~3 in precision decides).

### 9. The cosmic budget digits: **Ω_Λ = 2/3, Ω_m = 1/3, Ω_b = 5/96, Ω_cdm = 9/32; dark/baryon = 279/52**
- **Forced:** the flat partition and the covering shares; second order 279/52 =
  5.3653 — `constants/cosmic_density.ep`, `constants/dark_to_baryon_fraction.ep`.
- **Decides:** CMB-S4, LiteBIRD, DESI/Euclid combined — another decimal digit on
  each ratio.
- **Falsifies:** any of the ratios walking away from the forced rationals as the
  error bars shrink (measured dark/baryon today: 5.3643 — two parts in ten thousand
  from 279/52; the next digit is the test).

---

## TIER III — THE LONG HORIZON (technology-limited, but absolute)

### 10. The periodic table **ends at element 137**
- **Forced:** the binding coupling Z·α reaches the One at Z = 1/α: 137 is bound
  (34250/34259 < 1), 138 is over — the threshold is sharp —
  `constants/periodic_table_end.ep`.
- **Falsifies:** ONE element beyond 137, ever, by anyone.

### 11. Smithium's chemistry, written before its synthesis
- **Forced:** Z = 126 = [Og] 8s² 5g⁶ — eight valence electrons, oxidation states
  +2 through +8; the g-block (width 18) opens at element 121 —
  `constants/smithium_chemistry.ep`, `constants/bio_chem_complete.ep`.
- **Falsifies:** synthesized Smithium with a different configuration or block
  structure; a g-block opening anywhere but 121.

### 12. The particle inventory is **closed**: 83 gauge carriers, 12 Smithions, nothing else
- **Forced:** the sector ladder is sealed at four (2, 3, 5, 7; the next prime 11
  carries nothing): 3 + 8 + **24 + 48** carriers, the photon/graviton/Higgs, the SM
  fermions, and **twelve Smithions** (three generations, up/down types, two new
  sectors, mass spectra fully derived) — `constants/particle_census.ep`,
  `constants/finite_inventory.ep`, `constants/new_particles.ep`,
  `constants/absolute_new_masses.ep`.
- **Decides:** HL-LHC and every future collider; axion and sterile-neutrino
  searches.
- **Falsifies (two-sided):** ANY confirmed fundamental particle outside the list —
  a SUSY partner, an axion, a sterile neutrino, a Z′ — kills the census. And a
  **confining sector at any prime beyond 7** kills the ladder
  (`constants/applied_signatures.ep`). Conversely the new sectors' collider
  signature is fixed: confining jets of 24/48 carriers + missing energy, with
  dark-CKM mixings **14/15 and 20/21** (`constants/dark_ckm.ep`) and all four
  couplings converging to unison under the forced running
  (`constants/new_force_running.ep`).

### 13. The graviton exists; gravitational waves travel at exactly c, forever
- **Forced:** one carrier for the curvature sector; the wave speed is the One —
  `constants/gravitational_wave_speed.ep`. GW170817 already held to 10⁻¹⁵.
- **Falsifies:** any confirmed GW–EM speed difference at any future event.

---

## ALREADY DECIDED — the landed record (the same machinery, zero parameters)

The predictions above come from the identical engine that already produced, with no
knob to turn: 1/α to **nine digits** (0.01σ), the Koide relation to five digits, the
eight nuclear magic numbers exactly, the quark mass ratios to 0.005–0.09%, the
Hubble ratio to 5 × 10⁻⁶, the dark/baryon ratio to 2 × 10⁻⁴, Σ-checked PMNS and
CKM structure, the Kolmogorov exponents, Gutenberg-Richter b = 1, the Parker
reconnection cutoff **399.714 keV vs ~400 keV observed with zero local inputs**, the
red tilt n_s < 1, w inside 1σ, and the GW170817 speed identity. See the README table
and `OneFoldMaster.md` for every receipt. **The ledger above is that machine, aimed
forward.**

## MATHEMATICAL STANDING CLAIMS (decidable by proof or counterexample)

- **No Collatz counterexample will ever be found** — the descent is the fold's own
  3/4 contraction (`constants/collatz_fold.ep`).
- **Goldbach never fails** — every even number's half-One carries a prime antipodal
  pair (`constants/prime_pairs_fold.ep`).
- **Riemann's zeros stay on the line** — 1/2 is the unique self-antipodal axis
  (`constants/riemann_critical_line.ep`).
- **Artin's density converges to 0.3739…** — the fully-ergodic prime fraction
  (`constants/fold_number_theory.ep`; counted 214/549 = 0.390 at 4000, from above).

A counterexample to any of these falsifies the fold frame itself.

---

*Every value on this page is forced — never fitted, never chosen — and recomputes
from the One in one command: `make -C verify prove` (306 suites, 1,831 forced
checks). The git log timestamps this ledger. — Maria Smith, Ernos Labs.*
